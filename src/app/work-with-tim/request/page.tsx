import { RequestSection } from "../RequestSection";

export const metadata = { title: "Request a Discovery Call" };

export default function RequestPage() {
  return (
    <main className="wwt">
      <RequestSection headingLevel="h1" />
    </main>
  );
}
